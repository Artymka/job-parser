Подключение к telegram user api:
- добавить проверку нескольких proxy
- добавить retry middleware

Получение сообщений из каналов:
1. Перебираем все диалоги (пагинация не понадобится почти наверняка)
2. Для каждого диалга проверяем, является ли он каналом
3. Если это канал, читаем историю сообщений

Структура:
Есть тикер, который каждые 12 часов запускает функцию, которая возвращает последние сообщения из всех каналов (в виде массива сторк).
Эта функция по-новой устанавливает соединение с telegram user api, авторизуется, собирает список чатов, а только после собирает последние сообщения.
Продюсер закидывает полученные сообщения в kafka.

Также в качестве второй программы (процесса) крутится консьюмер, который каждые ~12 часов читает сообщения из kafka и просто выводит их в консоль.


Что нужно теперь:
- хранить не только текст сообщения, но и канал и какую-нибудь ссылку, чтобы можно было удобно переслать сообщение в телеграмме
- добавить сохранение информации о последних прочитанных сообщениях из каналов (чтобы знать, где остановились, и не дублировать отправку сообщений)
- посмотреть, есть ли бесплатный api у дипсика или какой-нибудь слабенькой llm (возможно, классификацию сообщений за нас будет выполнять llm)
- придумать, какие классы сообщений мы будем выделять и в какие топики будем их совать
- понять, какие способы рассылки у нас будут (например, telegram + email)
- сделать код более читаемым, проверить на наличие ошибок

Моделька, которую возможно будем использовать:
https://github.com/marketplace/models/azure-openai/o1-mini
Лимиты пока не ясны, но получилось достучаться до неё через python-скрипт с использованием библиотеки azure-ai-interface. Токен есть.

Будем хранить в postgres список каналов (название (для отображения) и username), из которых будем доставать сообщения.
Проходиться по каналам будем без помощи списка подписок, а с помощью resolveUsername в цикле.
Для каждого канала будем хранить id последнего прочитанного сообщения.
Объединим все модули в один, создадим там несколько докерфайлов, пакет с общим кодом.
